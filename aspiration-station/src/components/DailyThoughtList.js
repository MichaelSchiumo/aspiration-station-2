const DailyThoughtList = ({ thoughts }) => {
  //how should we display daily thoughts? Should it be a separate page?
  return <div>need to think of a design that makes sense for this</div>;
};

export default DailyThoughtList;
